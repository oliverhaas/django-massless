# =============================================================================
# DISABLED: PyPI publishing is intentionally turned off for now (pre-alpha).
# The entire workflow below is commented out. To enable releases later,
# uncomment this file (and tag.yml), then complete the PyPI Trusted Publisher
# and `pypi` GitHub environment setup.
#
# Before re-enabling: this is a Cython package, so the single `uv build` step
# below only produces an sdist plus one local-platform wheel. Replace it with a
# cibuildwheel matrix (ubuntu + macOS runners) that consumes [tool.cibuildwheel]
# in pyproject.toml, so cp312-cp314 platform wheels are built and uploaded.
# =============================================================================

# name: Publish to PyPI
#
# on:
#   push:
#     tags: ["v*"]
#   workflow_dispatch:
#     inputs:
#       version:
#         description: "Version to publish (e.g., 1.0.0)"
#         required: true
#
# jobs:
#   test:
#     runs-on: ubuntu-latest
#     steps:
#       - uses: actions/checkout@v6
#         with:
#           ref: ${{ github.event.inputs.version && format('v{0}', github.event.inputs.version) || github.ref }}
#       - uses: astral-sh/setup-uv@v7
#       - run: uv python install 3.12
#       - run: uv sync --group dev
#       - run: uv run ruff check
#       - run: uv run ruff format --check
#       - run: uv run pytest -n auto
#
#   publish:
#     needs: [test]
#     runs-on: ubuntu-latest
#     environment:
#       name: pypi
#       url: https://pypi.org/project/django-massless/
#     permissions:
#       id-token: write
#     steps:
#       - uses: actions/checkout@v6
#         with:
#           ref: ${{ github.event.inputs.version && format('v{0}', github.event.inputs.version) || github.ref }}
#       - uses: astral-sh/setup-uv@v7
#       - run: uv python install 3.12
#       - run: uv build
#       - uses: pypa/gh-action-pypi-publish@release/v1
